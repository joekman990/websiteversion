import 'package:flutter/material.dart';

class StrikeTheMatch extends StatelessWidget {
  const StrikeTheMatch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
